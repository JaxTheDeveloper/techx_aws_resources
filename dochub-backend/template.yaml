# This AWS SAM template has been generated from your function's configuration.
# If your function has one or more triggers, note that the AWS resources
# associated with these triggers aren't fully specified in this template and
# include placeholder values. Open this template in AWS Infrastructure Composer
# or your favorite IDE and modify it to specify a serverless application with
# other AWS resources.
#
# - SAM automatically generates permissions to interact with the resources
#   defined in this template. If your function needs additional permissions, add
#   Policies to the AWS::Serverless::Function resource.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model (SAM) template describing your function.
Resources:
  dochubbackend:
    Type: AWS::Serverless::Function
    Properties:
      Architectures:
        - x86_64
      CodeUri: ./src
      Description: ''
      Environment:
        Variables:
          AI_MODEL_ID: >-
            arn:aws:bedrock:us-west-2:399434941001:inference-profile/global.anthropic.claude-haiku-4-5-20251001-v1:0
          USERSTORE_BACKEND: dynamodb
          VECTOR_BACKEND: bedrock_kb
          VECTOR_BEDROCK_KB_ID: X2ZTYMOW4L
          SERVE_FRONTEND: 'false'
          USERSTORE_TABLE: dochub-docs
          STORAGE_BUCKET: dochub-docss
          STORAGE_BACKEND: s3
          CORS_ORIGINS: https://docs4hub.tech,https://d1234abcd.cloudfront.net
          AI_BACKEND: bedrock
      EphemeralStorage:
        Size: 512
      Handler: src.app.handler
      Layers:
        - arn:aws:lambda:us-west-2:399434941001:layer:dochub-dependencies:1
      LoggingConfig:
        LogFormat: Text
        LogGroup: /aws/lambda/dochub-backend
      MemorySize: 512
      PackageType: Zip
      RecursiveLoop: Terminate
      Runtime: python3.12
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
      SnapStart:
        ApplyOn: None
      Tags:
        Environment: hackathon
        Owner: quangphongnguyen147@gmail.com
        Project: w7capstone
        Team: g1
      Timeout: 60
      Tracing: PassThrough
      VpcConfig:
        Ipv6AllowedForDualStack: false
        SecurityGroupIds:
          - sg-0834b55d2695d4c15
        SubnetIds:
          - subnet-0a4af6147307cb3bc
          - subnet-02d0ef66e9f6ed24b
      Events:
        Trigger1Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /dochub-backend
        Trigger2Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /docs/list
        Trigger3Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /query
        Trigger4Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /upload
        Trigger5Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /health
